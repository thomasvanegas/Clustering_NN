#Importacion de librerias, paquetes y clases.
import heapq
from Punto2D import Punto2D
from leerPuntos import leerPuntos

class Clustering:
    """
        Clase que representando un cluster (Conjunto de puntos cercanos)
    """

    def __init__(self, punto: 'Punto2D'):
        pass

    def clusterizar() -> int:
        num_clusters = 0
        return num_clusters